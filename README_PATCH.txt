O365 password toggle patch

Files included:
- src/main/resources/templates/refer.html
- src/main/resources/static/css/refer_github.css

How to apply:
1) Unzip this package at the root of your local repo (e.g. D:\git\o365)
   so paths match and files overwrite.
2) Or manually copy the two files to the same paths in your repo.
3) Commit and push.

Change:
- Adds a small eye button inside password inputs on the public registration page
  to toggle password visibility (show/hide), with hover/focus styles.
